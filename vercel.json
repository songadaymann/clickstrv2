{
  "buildCommand": "if [ -f src-ts/package.json ]; then npm --prefix src-ts install && npm --prefix src-ts run build && rm -rf dist && cp -R src-ts/dist dist; else npm install && npm run build; fi",
  "outputDirectory": "dist",
  "framework": "vite"
}
