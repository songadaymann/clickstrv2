todo:
[x] we should see if we can create a front end clicking bot that can defeat our turnstile etc.
[x] leaderboard needs to change. we no longer have bots competing so we can take away that part. make it so we just have 3 leaderboards "current epoch", "all time clicks", and "all time earned".
[x] in global stats module, take away "bots" since we not longer have them
[x] make the mint rewards panel always present (not just showing up when we first mint)
[x] fix the mint modal that pops up - the ??? mystery rewards flow outside the container of the modal?
[x] add the mint buttons to cursors we've unlocked but not minted to the modal
[x] make sure those mint buttons show up in the modal on mobile (otherwise we can't mint)
[x] the mint modal for minting an individual nft reward needs to be on a higher plane than the full mint rewards modal (so we can see it on mobile)